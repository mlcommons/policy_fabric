{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ProjectOwnershipCredential",
  "title": "ProjectOwnershipCredential",
  "description": "An entity owns/manages a project; issuer claims the subject owns the project.",
  "type": "object",
  "properties": {
    "Owns": {
      "type": "string",
      "description": "DID of the project the subject owns."
    }
  },
  "required": ["Owns"]
}
