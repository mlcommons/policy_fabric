{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "TeamCredential",
  "title": "TeamCredential",
  "description": "A user-PI binding; issuer claims the subject is a member of the team of a PI.",
  "type": "object",
  "properties": {
    "MemberOfTeamOf": {
      "type": "string",
      "description": "DID of the PI whose team the subject is a member of."
    }
  },
  "required": ["MemberOfTeamOf"]
}
