{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "EmailCredential",
  "title": "EmailCredential",
  "description": "Evidence of the requester's verified email; issuer claims the subject controls an email address.",
  "type": "object",
  "properties": {
    "emailAddress": {
      "type": "string",
      "format": "email",
      "description": "Email address controlled by the subject."
    }
  },
  "required": ["emailAddress"]
}
