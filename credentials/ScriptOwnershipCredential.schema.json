{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ScriptOwnershipCredential",
  "title": "ScriptOwnershipCredential",
  "description": "A wallet's claim over a script; the issuing wallet claims the script, which is the credential subject (named by its DID), is owned by that same wallet.",
  "type": "object",
  "properties": {
    "ownedBy": {
      "type": "string",
      "description": "DID of the wallet that owns the script. The wallet asserts this about itself, so it must match the credential's issuer."
    }
  },
  "required": ["ownedBy"]
}
