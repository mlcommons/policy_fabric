{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ScriptHashCredential",
  "title": "ScriptHashCredential",
  "description": "The digest of a script; issuer claims the script, which is the credential subject (named by its DID), has the stated content digest.",
  "type": "object",
  "properties": {
    "scriptHash": {
      "type": "string",
      "description": "Digest of the script's content, prefixed by its algorithm (e.g. sha256:<hex>)."
    }
  },
  "required": ["scriptHash"]
}
