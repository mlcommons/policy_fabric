{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "WalletVerifyingKeyCredential",
  "title": "WalletVerifyingKeyCredential",
  "description": "A wallet-to-verifying-key binding; issuer (a wallet key authority) claims the wallet, which is the credential subject (named by its did:pdo DID), is associated with a verifying key it is registered with on the ledger.",
  "type": "object",
  "properties": {
    "verifying_key": {
      "type": "string",
      "description": "PEM-encoded verifying key the wallet contract is registered with on the ledger."
    }
  },
  "required": ["verifying_key"]
}
