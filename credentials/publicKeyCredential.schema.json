{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "publicKeyCredential",
  "title": "publicKeyCredential",
  "description": "The requester's channel public key; issuer claims the subject controls a public key used to receive encrypted data.",
  "type": "object",
  "properties": {
    "key": {
      "type": "string",
      "description": "PEM-encoded public key the data is encrypted to (the channel key)."
    }
  },
  "required": ["key"]
}
