{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "IntendedDataUseCredential",
  "title": "IntendedDataUseCredential",
  "description": "A document describing the intended use of datasets by a project; issuer (DAC) claims the subject intends to use datasets for listed purposes and diseases.",
  "type": "object",
  "properties": {
    "useOnlyFor": {
      "type": "object",
      "description": "The purposes and diseases for which the datasets are intended to be used.",
      "properties": {
        "purposes": {
          "type": "array",
          "description": "List of intended-use purposes.",
          "items": {
            "type": "string"
          }
        },
        "diseases": {
          "type": "array",
          "description": "List of diseases, expressed as MONDO codes.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["purposes", "diseases"]
    }
  },
  "required": ["useOnlyFor"]
}
